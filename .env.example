# Local Development Environment Variables
# Copy this file to .env and fill in the values
# WARNING: Do not commit .env to version control!
#
# NOTE: All values are optional. Push notifications will be disabled if not configured.
# The app will work without these for most development tasks.

# =============================================================================
# Azure Service Bus (Required only for push notification testing)
# =============================================================================
# Get the connection string using Azure CLI:
#   Windows (PowerShell):
#     az servicebus namespace authorization-rule keys list `
#       --resource-group growth-tracker `
#       --namespace-name growthtrackerservicebus `
#       --name RootManageSharedAccessKey `
#       --query primaryConnectionString -o tsv
#
#   macOS/Linux (Bash):
#     az servicebus namespace authorization-rule keys list \
#       --resource-group growth-tracker \
#       --namespace-name growthtrackerservicebus \
#       --name RootManageSharedAccessKey \
#       --query primaryConnectionString -o tsv
#
AZURE_SERVICEBUS_CONNECTION_STRING=

# =============================================================================
# VAPID Keys (Required only for push notification testing)
# =============================================================================
# Generate new keys (any platform):
#   npx web-push generate-vapid-keys
#
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
