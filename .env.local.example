# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local if you want to customize local dev settings.
# WARNING: These are for LOCAL DEVELOPMENT ONLY. Never use production values.
# =============================================================================

# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ DO NOT PUT PRODUCTION CREDENTIALS IN THIS FILE OR .env.local               │
# │ This file is checked into version control as a reference template.         │
# └─────────────────────────────────────────────────────────────────────────────┘

# Database - Uses Docker Compose postgres service
DB_HOST=localhost
DB_PORT=5432
DB_NAME=growth_tracker_dev
DB_USERNAME=localdev
DB_PASSWORD=localdevpassword
DB_SSLMODE=disable

# Redis - Uses Docker Compose redis service
REDIS_URL=redis://localhost:6379

# JWT - Development only (NOT a production secret!)
JWT_SECRET_KEY=local-dev-secret-key-do-not-use-in-production

# Environment
ENV=development
PORT=8000
FRONTEND_BASE_URL=http://localhost:5173

# Azure Storage - Azurite emulator (well-known dev credentials)
# These are the default Azurite credentials (publicly documented)
AZURE_STORAGE_ACCOUNT_NAME=devstoreaccount1
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://localhost:10000/devstoreaccount1;
AZURE_STORAGE_CONTAINER=profile-pictures

# Email - Mailpit SMTP (leave RESEND_API_KEY empty to use SMTP)
RESEND_API_KEY=
SMTP_HOST=localhost
SMTP_PORT=1025
EMAIL_FROM_ADDRESS=noreply@localhost.dev
EMAIL_FROM_NAME=Growth Tracker Local

# Frontend
VITE_API_URL=http://localhost:8000

# =============================================================================
# OPTIONAL SERVICES (Disabled in local dev)
# =============================================================================

# Axiom Logging - Leave empty for local dev
AXIOM_API_TOKEN=
AXIOM_DATASET=
