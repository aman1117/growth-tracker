# =============================================================================
# Growth Tracker Backend - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# Never commit your actual .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=8080
ENV=development  # Options: development, production
FRONTEND_BASE_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# Option 1: Full DATABASE_URL (takes precedence if set)
# DATABASE_URL=postgres://username:password@localhost:5432/growth_tracker?sslmode=disable
# DATABASE_URL=postgres://username:password@your-project.neon.tech/growth_tracker?sslmode=require

# Option 2: Individual components (used if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=growth_tracker
DB_USERNAME=postgres
DB_PASSWORD=password
DB_SSLMODE=require

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# Generate a strong secret: openssl rand -base64 32
# IMPORTANT: Use different secrets for each environment
JWT_SECRET_KEY=your-super-secure-jwt-secret-change-me
TTL_ACCESS_TOKEN=1440  # Minutes (24 hours = 1440)
TTL_REFRESH_TOKEN=10080  # Minutes (7 days = 10080)

# -----------------------------------------------------------------------------
# Redis Configuration (Optional - for password reset functionality)
# -----------------------------------------------------------------------------
# Leave empty to disable password reset feature
# Example: redis://default:password@redis-host:6379
REDIS_URL=

# -----------------------------------------------------------------------------
# Azure Blob Storage Configuration (Optional - for profile pictures)
# -----------------------------------------------------------------------------
# Leave empty to disable profile picture upload
# Get connection string from Azure Portal > Storage Account > Access Keys
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_CONTAINER=profile-pictures

# -----------------------------------------------------------------------------
# Email Service Configuration (Optional - for email notifications)
# -----------------------------------------------------------------------------
# Uses Resend API (https://resend.com)
# Leave empty to disable email functionality
RESEND_API_KEY=
EMAIL_FROM_ADDRESS=noreply@yourdomain.com
EMAIL_FROM_NAME=Growth Tracker

# -----------------------------------------------------------------------------
# Logging Configuration (Optional - for cloud logging)
# -----------------------------------------------------------------------------
# Uses Axiom (https://axiom.co) for log aggregation
# Leave empty for local console logging only
AXIOM_API_TOKEN=
AXIOM_DATASET=growth-tracker-logs

# =============================================================================
# Quick Start Guide
# =============================================================================
# 
# Minimum required configuration:
#   1. DB_* variables OR DATABASE_URL - PostgreSQL connection
#   2. JWT_SECRET_KEY - Secure random string for token signing
#
# For production, also configure:
#   - ENV=production
#   - REDIS_URL for password reset
#   - AZURE_STORAGE_* for profile pictures
#   - RESEND_API_KEY for email notifications
#   - AXIOM_* for cloud logging
#
# =============================================================================
