# Development API configuration
VITE_API_URL=http://localhost:8000

# GoodLogs public key (browser-safe, events only)
# Production key goes in .env.production; override in CI if needed.
VITE_GOODLOGS_PUBLIC_KEY=

# Production builds use frontend/.env.production by default.
# Override VITE_API_URL in your deployment environment only when needed.
